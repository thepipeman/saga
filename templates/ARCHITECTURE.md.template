# Architecture

> Generated by /init-context. Edit freely — this is a living reference, not a generated artifact to be overwritten blindly on every refresh (review the diff on --refresh before accepting it).

## Services / modules

<!-- List each service/module, its responsibility, and how it talks to the others. -->

## Request flow

<!-- Controller -> Service -> Repository, or however this codebase actually layers things. Note any exceptions. -->

## Data stores

<!-- Postgres schemas/databases in use, and which service owns which tables. -->

## Cross-cutting concerns

<!-- Observability, auth, idempotency handling, anything that spans multiple services. -->

## Known deviations from the pattern above

<!-- Be honest here — every real codebase has a few. Better to name them than pretend they don't exist. -->
